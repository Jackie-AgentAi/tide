# v0.2.0 更新说明

**发布日期：** 2026-05-14  
**标签：** `v0.2.0`

本版本聚焦四个主 Tab（睡眠 / 专注 / 呼吸 / 冥想）的界面还原、布局与数据展示，以及睡眠模块封面加载策略。

---

## 睡眠 Sleep

- 列表与迷你播放器、播放页封面：**优先使用接口 `cover` 远程图**，无 `cover` 时再回退本地占位图（`resolveSleepImageSource`），避免名称与图错位。
- 迷你播放器区域底部留白加大，减轻与最后一行卡片的遮挡。
- 选中态与播放角标层级优化；可选加载 **马善政** 手写体（`@expo-google-fonts/ma-shan-zheng`）用于部分副标题/曲名展示（加载完成前回退系统字体）。

## 专注 Focus

- 按设计稿重排：薄荷渐变底、右上粉雾装饰、酒红主色、中央番茄钟卡片（分段器 / 大倒计时 / 时长行 / 开始·结束）。
- **整页不再纵向滚动**；仅「背景音」横向滑动。
- 番茄模式与全局 `sessionStore.focusPreset` 同步；背景音条目前四项与 `sounds` 前四条对齐（远程封面 + 名称）。
- 中部弹性占位 + 底部说明区，迷你条贴近 Tab；背景图分层与透明度调整。

## 呼吸 Breathe

- 顶栏标题 + 齿轮、横向三枚模式卡、中央光晕与呼吸球、底部浅色说明条、「结束练习」链接。
- 修复横向 `ScrollView` 在纵向 flex 中过高导致的叠层与白块问题；中央文案纵向排版，避免「吸气 / 跟随节奏」重叠。

## 冥想 Meditate

- 固定顶图（月亮横幅）+ 标题；**仅中间两列课程网格可纵向滚动**；底部「开始」贴近 Tab 栏。
- 头图高度与封面边长按屏高计算，尽量 **一屏展示两行卡片**；减弱叠色、降低全屏底图透明度以突出头图氛围。
- 卡片选中粉边框；「开始」使用粉渐变胶囊按钮，并按选中课程跳转完成页。

## 依赖与工程

- 新增：`@expo-google-fonts/ma-shan-zheng`（睡眠页手写风格，可选）。
- 已通过：`npm run typecheck`、`npm run lint`。

---

## 字体说明（与产品期望的对照）

| 期望 | 当前实现 |
|------|----------|
| 标题类圆润黑体（思源柔黑 / 圆体） | 仍主要为 **系统默认无衬线**（Android 多为 Roboto / 厂商字体；iOS 为 San Francisco）。 |
| 正文 / 次级信息（思源黑体 Regular / 苹方） | **系统默认**，未全局嵌入 Noto Sans SC。 |
| 按钮 / 卡片字重略加重 | 通过 **`fontWeight`** 区分，未单独打包 Medium 字重文件。 |
| 数字圆润 | 系统数字字形；未单独嵌入 Nunito / Montserrat Rounded。 |

**若要完全对齐「思源柔黑 + 思源黑体」商用方案：** 需在仓库中放入对应 `.ttf/.otf`（注意授权），在根 `app/_layout.tsx` 用 `expo-font` 的 `useFonts` 预加载后，在 `theme` 或各页 `Text` 上设置 `fontFamily`。本标签版本仅包含睡眠页可选 **马善政** 作为局部手写点缀。

---

## 升级与构建

```bash
git checkout v0.2.0
npm ci
npm run typecheck
npx expo start
```

---

## 已知后续可做

- 全局 `fontFamily` 与字重映射（若确定字体文件与授权）。
- `app.json` / `package.json` 的 `version` 字段是否与 `v0.2.0` 对齐（当前未强制修改）。
