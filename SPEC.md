# SPEC.md — 产品需求与验收标准

本文约定「干什么」与「如何验收」。实现细节与环境约束见 `AGENTS.md`。

---

## 1. 产品定位

- **类型**：潮汐类放松 App — 睡眠、专注、呼吸、冥想。
- **气质**：安静、克制、沉浸、治愈；打开 App **直接进入可用功能**，不做营销首页。

---

## 2. 范围与优先级

| 项目 | 说明 |
|------|------|
| 当前交付重点 | **Android**（模拟器或真机） |
| 后期 | 保留 **iOS 17+** 适配可能；当前不要求 iOS 打包 |
| 仓库 | 代码托管 **GitHub** |

---

## 3. 信息架构与导航

### 3.1 底部 Tab（必须 4 个）

1. 睡眠 **Sleep**
2. 专注 **Focus**
3. 呼吸 **Breathe**
4. 冥想 **Meditate**

### 3.2 路由（Expo Router）

- 底部 Tab + Stack：睡眠播放页、呼吸训练页、冥想详情、冥想引导等以 Stack 呈现。
- **Android 返回键**：从详情回列表、从训练/播放页回上一级；**不导致音频状态混乱**。
- 当前播放状态在相关界面中有**清晰反馈**。

---

## 4. 数据源与类型

### 4.1 API

- **URL**：`https://zzz-pet.oss-cn-hangzhou.aliyuncs.com/api/sounds.json`
- **必须**：为该响应定义**完整 TypeScript 类型**，并实现独立的数据请求服务（如 `soundsApi`）。

### 4.2 数据结构（逻辑）

```json
{
  "alarm": {
    "id": "morning_joy",
    "name": "Morning Joy",
    "url": "https://....mp3",
    "cover": ""
  },
  "sounds": [
    {
      "id": "cat_purr_1",
      "name": "猫呼噜声",
      "url": "https://....m4a",
      "cover": "https://....jpg"
    }
  ]
}
```

### 4.3 使用规则

| 数据 | 用途 |
|------|------|
| `alarm` | 睡眠模块**闹钟音乐** |
| `sounds` | 睡眠**白噪音列表**；专注**背景音列表** |
| 冥想 | 可使用**静态数据**，不强制依赖 API |

### 4.4 网络体验（全局）

所有依赖该 API 的界面必须包含：

- 加载中
- 失败提示
- 失败重试
- 空数据兜底

且须避免因缓存策略导致**长期拿不到新数据**（实现上需可说明）。

### 4.5 媒体展示

- 封面图 **1:1**，填充裁剪；与显示区域一致。
- `cover` 为空：优雅占位。

---

## 5. 音频全局规则

- **同一时刻仅播放一路音频**。
- **切换音频**：停止上一路再播放新一路。
- **页面切换**：不莫名停止，除非用户停止或业务流程要求；**不得多路叠加**。
- 加载失败：**UI 提示**。
- 全局播放状态在 Tab/Stack 间**可辨识**。

---

## 6. 功能需求

### 6.1 A. 睡眠 Sleep

#### 声音列表

- **布局**：`sounds` **两列网格**。
- **卡片**：封面 + 名称文字叠在封面**中心**。
- **点击**：进入睡眠播放页。

#### 播放页

- 展示：封面、名称、播放/暂停、返回。
- 白噪音：**循环播放**。
- 用户切换声音：**停旧播新**；与**全局音频状态**同步。

#### 睡眠定时（按「醒来时间」）

- 用户选择醒来时间后开始倒计时；界面显示**剩余时间**。
- 若所选时间**已过当天**：默认视为**次日同一时间**。
- **到点后**：
  - 自动停止白噪音；
  - 弹窗文案：**「该起床了」**；
  - 播放 `alarm`，**循环**，**最长 20 分钟**；
  - 弹窗仅一个按钮：**「停止闹钟」** — 点击后立即停 alarm 并结束流程；
  - 若用户未操作，**20 分钟后**自动停 alarm 并结束流程。

---

### 6.2 B. 专注 Focus

#### 番茄钟

- **模式 1**：25 分钟专注 / 5 分钟休息  
- **模式 2**：50 分钟专注 / 10 分钟休息  
- 界面：当前阶段（专注/休息）、剩余时间、开始 / 暂停 / 结束。

#### 背景音

- 从 `sounds` 中选一首作为专注背景音。
- **开始专注**时自动播放背景音；**暂停或结束**时停止背景音。
- **休息阶段**：停背景音或保持安静，**逻辑自洽**即可。

#### 沉浸模式（简单版）

- 开关控制。
- **开启时**：若在**专注期间** App **进入后台**，则：
  - 判定本次专注**失败**并提示用户；
  - **停止计时**；
  - **停止背景音**。  
- 可使用 React Native `AppState` 实现。

---

### 6.3 C. 呼吸 Breathe

#### 练习入口（3 个）

| 模式 | 节奏 | 适用说明（展示给用户） |
|------|------|-------------------------|
| 4-4-4-4 | 吸 4 / 屏 4 / 呼 4 / 屏 4，循环 | 紧张、焦虑，需快速冷静、稳住情绪 |
| 4-7-8 | 吸 4 / 屏 7 / 呼 8，循环 | 睡前放松，助眠 |
| 5-5 | 吸 5 / 呼 5，循环 | 日常减压，恢复平静与专注 |

#### 训练页（三模式共用）

- 显示：当前阶段（吸气/屏息/呼气）、**圆球**、圆球内**当前阶段倒计时秒数**、**本次训练已进行时长**。
- **圆球动画**：吸气变大；屏息大小不变；呼气变小。
- 按钮：开始、暂停、结束。
- 结束后回到**呼吸入口列表**。
- 动画：**跨平台**方案，避免 iOS/Android 表现差异过大。

---

### 6.4 D. 冥想 Meditate

#### 静态列表（4 条）

- 快速入眠  
- 考试压力  
- 呼吸练习  
- 身体扫描  

每条：**标题** + **一句简介**。

#### 详情页

- 标题、简介、「开始」按钮。

#### 引导页（最简）

- 按**段落**展示引导文字；每主题 **4～6 段**。
- 操作：**下一段**、**上一段**、**结束**。
- 结束后回到详情或列表，**流程简单一致**。

---

## 7. 状态管理（最低建议）

实现层应能表达并驱动以下状态（可与 Context/zustand 等映射一致）：

- 当前音频；播放类型：`sleep` / `focus` / `alarm` / `none`
- 是否播放中；是否加载中；音频错误
- 睡眠：醒来时间、剩余时间；alarm 是否正在播放
- 专注：当前模式（25/5 或 50/10）、当前阶段（专注/休息）、剩余时间、沉浸模式开关
- 呼吸：当前模式、当前阶段、已训练时长
- 冥想：当前段落索引

---

## 8. UI / 视觉验收要点

- **睡眠**：偏夜晚、安静、低亮度。  
- **专注**：清爽、克制。  
- **呼吸**：柔和、留白足。  
- **冥想**：简洁、舒缓。  
- 整体偏梦幻；圆角、间距、字体层级统一；移动端无文字溢出、元素重叠、底部按钮被遮挡。

---

## 9. 验收标准（清单）

以下为发布前需满足的**可验证**条目：

- [ ] `expo start` 可启动 App。
- [ ] Android 模拟器或真机正常运行。
- [ ] 能从 API 拉取 `sounds` 与 `alarm`。
- [ ] 网络：加载、失败重试、空数据可用。
- [ ] 睡眠：两列网格列表可用。
- [ ] 睡眠播放：播放/暂停、白噪音循环；切换声音停旧播新。
- [ ] 睡眠定时：醒来时间倒计时正确；过期时间按「次日」理解。
- [ ] 到点：停白噪音 → 弹窗「该起床了」→ 播放 alarm（循环，≤20 分钟）→ 「停止闹钟」与 20 分钟自动停止均符合。
- [ ] 专注：25/5 与 50/10 可用；背景音随专注开始/暂停/结束一致。
- [ ] 沉浸模式：专注中途进后台 → 失败提示 + 停计时 + 停背景音。
- [ ] 呼吸：三种模式进入训练页；圆球动画、阶段倒计时、训练时长可用。
- [ ] 冥想：列表、详情、多段引导与导航可用。
- [ ] 底部 Tab 与 Stack 正常；Android 返回键合理。
- [ ] 常见 Android 屏宽下 UI 无严重重叠、溢出、遮挡。
- [ ] TypeScript 无明显类型错误；结构清晰，利于后续 iOS 扩展。

---

## 10. 交付物（每次迭代）

1. **可运行代码**（含本次范围内的交互，非纯静态）。
2. **独立变更日志文件**：列出新增/修改文件及涉及页面。
3. 保证迭代后项目**仍可交付运行**。

---

*文档日期：2026-05-12*
